name: V96 Symbol Role Engine Screen

on:
  push:
    branches:
      - research/v96-symbol-missed-profit-attribution
    paths:
      - scripts/research_lab_v96_symbol_role_engine_screen.py
      - scripts/research_lab_v96_symbol_missed_profit_attribution.py
      - .github/workflows/v96-symbol-role-engine-screen.yml
  pull_request:
    branches:
      - research/v96-core-loss-audit-historical
    paths:
      - scripts/research_lab_v96_symbol_role_engine_screen.py
      - scripts/research_lab_v96_symbol_missed_profit_attribution.py
      - .github/workflows/v96-symbol-role-engine-screen.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  screen:
    runs-on: ubuntu-latest
    timeout-minutes: 75
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Syntax check
        run: |
          python -m py_compile scripts/research_lab_v96_symbol_missed_profit_attribution.py
          python -m py_compile scripts/research_lab_v96_symbol_role_engine_screen.py
      - name: Run fixed symbol role engine screen
        env:
          RESEARCH_AUTONOMOUS_STATE_DIR: .research-state
        run: python scripts/research_lab_v96_symbol_role_engine_screen.py
      - name: Upload role engine evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: v96-symbol-role-engine-screen
          path: |
            .research-state/v96-symbol-role-engine-screen.json
            .research-state/v96-symbol-role-engine-screen.md
          if-no-files-found: warn
          retention-days: 90

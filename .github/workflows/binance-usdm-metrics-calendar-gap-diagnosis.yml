name: Binance USD-M Metrics Calendar Gap Diagnosis

on:
  workflow_dispatch:
  push:
    branches:
      - chatgpt/3y-entry-edge-diagnosis-20260815
    paths:
      - scripts/research_probe_binance_usdm_metrics_calendar.py
      - .github/workflows/binance-usdm-metrics-calendar-gap-diagnosis.yml

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Assert frozen calendar probe
        shell: bash
        run: |
          set -euo pipefail
          test "$(git hash-object scripts/research_probe_binance_usdm_metrics_calendar.py)" = "b00cd188557f4f680f61d417fc8b81b7c766582a"
          echo METRICS_CALENDAR_PROBE_SOURCE_PASS
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Compile probe
        run: python -m py_compile scripts/research_probe_binance_usdm_metrics_calendar.py
      - name: Diagnose design-period archive calendar
        run: python scripts/research_probe_binance_usdm_metrics_calendar.py
      - name: Assert probe-only boundary
        shell: bash
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          d=json.loads(Path('.research-state/binance-usdm-metrics-calendar-gap-diagnosis.json').read_text())
          assert d['researchOnly'] is True
          assert d['strategyEvaluationPerformed'] is False
          assert d['productionChanged'] is False
          assert d['vpsChanged'] is False
          assert d['liveChanged'] is False
          assert d['realTradingEnabled'] is False
          assert d['freshOosRead'] is False
          assert d['post20260701DataUsed'] is False
          assert d['endExclusive'] == '2026-07-01'
          print('METRICS_CALENDAR_PROBE_ONLY_PASS')
          PY
      - name: Upload calendar diagnosis
        uses: actions/upload-artifact@v4
        with:
          name: binance-usdm-metrics-calendar-gap-diagnosis
          path: |
            .research-state/binance-usdm-metrics-calendar-gap-diagnosis.json
            .research-state/binance-usdm-metrics-calendar-gap-diagnosis.md
          if-no-files-found: error
          retention-days: 30

name: Six Pair Rolling 1Y V57

on:
  workflow_dispatch:
  push:
    branches:
      - research/win80-profit-optimization-v1
    paths:
      - "research/six-pair-rolling-1y-v57-trigger.txt"
      - "scripts/research_lab_six_pair_1y_v57.py"
      - "scripts/research_lab_distinct_logic_tournament_v56.py"
      - "scripts/research_lab_parallel_event_regime_v53.py"
      - "scripts/research-lab-prefetch-event-regime-v53.ts"
      - "lib/research-lab/perp/**"
      - ".github/workflows/six-pair-rolling-1y-v57.yml"

permissions:
  contents: read

concurrency:
  group: six-pair-rolling-1y-v57-${{ github.ref }}
  cancel-in-progress: true

jobs:
  research:
    strategy:
      fail-fast: false
      max-parallel: 12
      matrix:
        family:
          - trend_breakout
          - shock_mean_reversion
          - rs_rotation
          - residual_statarb
          - carry_hedged
          - crash_rebound
          - session_opening_range
          - compression_expansion
          - lead_lag
          - orderflow_depth
          - pair_cointegration
          - breadth_capitulation
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Restore public USD-M cache
        uses: actions/cache@v4
        with:
          path: .cache/perp-research-usdm
          key: six-pair-rolling-1y-v57-usdm-${{ hashFiles('lib/research-lab/perp/futures-data-source.ts') }}
          restore-keys: |
            distinct-tournament-v56-usdm-
            event-regime-v53-usdm-
            parallel-trend-vol-v53-usdm-
            win80-profit-v1-usdm-
            autonomous-usdm-v2-
      - name: Install dependencies
        run: npm ci
      - name: Prefetch genuine public USD-M history
        run: npx tsx scripts/research-lab-prefetch-event-regime-v53.ts
      - name: Compile research source
        run: python -m py_compile scripts/research_lab_six_pair_1y_v57.py scripts/research_lab_distinct_logic_tournament_v56.py scripts/research_lab_parallel_event_regime_v53.py
      - name: Run fixed-six rolling-one-year family
        env:
          RESEARCH_AUTONOMOUS_STATE_DIR: .research-state
        run: python scripts/research_lab_six_pair_1y_v57.py --family "${{ matrix.family }}"
      - name: Upload family evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: six-pair-rolling-1y-v57-${{ matrix.family }}
          path: |
            .research-state/six-pair-rolling-1y-v57-${{ matrix.family }}.json
            .research-state/six-pair-rolling-1y-v57-${{ matrix.family }}.md
          if-no-files-found: warn
          retention-days: 30

# Research only. Fixed BTC/ETH/BNB/SOL/LINK/AVAX universe, latest genuine 365d only.
# No production, VPS, credentials, orders, accounts, positions, approvals or LIVE changes.

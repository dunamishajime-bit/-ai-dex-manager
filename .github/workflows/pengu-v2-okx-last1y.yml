name: PENGU V2 OKX Last 1Y

on:
  workflow_dispatch:
  push:
    branches:
      - research/pengu-dual-ls-v2-20260810
    paths:
      - 'scripts/research/pengu_v2_okx_last1y.py'
      - '.github/workflows/pengu-v2-okx-last1y.yml'

permissions:
  contents: read

jobs:
  last1y:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check -q numpy pandas
      - name: Syntax check
        run: python -m py_compile scripts/research/pengu_v2_okx_frozen.py scripts/research/pengu_v2_okx_last1y.py
      - name: Run exact last-1y frozen BT
        run: python scripts/research/pengu_v2_okx_last1y.py
      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pengu-v2-okx-last1y-${{ github.sha }}
          path: research/pengu-v2-okx-last1y/
          if-no-files-found: warn
          retention-days: 30

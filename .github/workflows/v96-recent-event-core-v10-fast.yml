name: V96 Recent Event Core V10 Online Meta Router Fast
on:
  workflow_dispatch:
  push:
    branches: [research/v96-recent-regime-redesign-final2]
    paths:
      - 'scripts/research_lab_v96_recent_event_core_v10.py'
      - 'scripts/research_lab_v96_recent_event_core_v10_fast_runner.py'
      - '.github/workflows/v96-recent-event-core-v10-fast.yml'
permissions:
  contents: read
jobs:
  research:
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
        with: {ref: research/v96-v52-dual-slot-one-year-bt, path: .research-base, fetch-depth: 1}
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - uses: actions/cache@v4
        with:
          path: |
            .cache/perp-research-usdm
            .research-base/.cache/perp-research-usdm
          key: v96-v10-fast-${{ runner.os }}-${{ hashFiles('scripts/research_lab_v96_recent_event_core_v10.py', 'scripts/research_lab_v96_recent_event_core_v10_fast_runner.py') }}
          restore-keys: |
            v96-v10-${{ runner.os }}-
            v96-v6-fast-${{ runner.os }}-
            v96-v52-dual-slot-one-year-${{ runner.os }}-
      - name: Compile
        run: python -m py_compile scripts/research_lab_v96_recent_event_core_v6.py scripts/research_lab_v96_recent_event_core_v10.py scripts/research_lab_v96_recent_event_core_v10_fast_runner.py
      - name: Run cached online shadow meta-router
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .research-state/v96-v10-fast
          python scripts/research_lab_v96_recent_event_core_v10_fast_runner.py --output-dir .research-state/v96-v10-fast 2>&1 | tee .research-state/v96-v10-fast/v10.log
          test -s .research-state/v96-v10-fast/v96-recent-event-core-v10.json
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: v96-v10-fast-${{ github.run_id }}
          path: .research-state/v96-v10-fast
          retention-days: 90

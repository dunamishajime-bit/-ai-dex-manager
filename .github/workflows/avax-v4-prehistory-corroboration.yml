name: AVAX V4 Prehistory Corroboration

on:
  workflow_dispatch:
  push:
    branches:
      - chatgpt/3y-entry-edge-diagnosis-20260815
    paths:
      - scripts/research_avax_v4_prehistory_corroboration.py
      - .github/workflows/avax-v4-prehistory-corroboration.yml

concurrency:
  group: avax-v4-prehistory-corroboration-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Assert frozen V4 source
        shell: bash
        run: |
          set -euo pipefail
          test "$(git hash-object scripts/research_causal_handoff_clean_sheet_v4.py)" = "83bb895bfc865fbc0cc60f3e84789675101c2fb6"
          test "$(git hash-object scripts/research_avax_v4_prehistory_corroboration.py)" = "fe6e2b8a4ca1eee14ad7e5e75b655eb902ddeec3"
          echo FROZEN_AVAX_V4_SOURCE_PASS

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Restore exact research cache
        uses: actions/cache@v4
        with:
          path: .cache/perp-research-usdm
          key: pairwise-clean-sheet-3y-usdm-v1-20260815
          restore-keys: |
            pair-specific-v107-exact-usdm-v3-20260811
            pair-specific-v104-exact-usdm-v3-20260810

      - name: Check exact inputs
        id: exact
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          import os
          root=Path('.cache/perp-research-usdm/consolidated')
          symbols=('BTC','SOL','LINK','ETH','BNB','AVAX')
          required=[root/f'{s}USDT-1661990400000-1782864000000-v3.json' for s in symbols]
          ready=all(p.is_file() for p in required)
          with open(os.environ['GITHUB_OUTPUT'],'a',encoding='utf-8') as fh:
              fh.write(f"ready={'true' if ready else 'false'}\n")
          print('EXACT_CACHE_READY' if ready else 'EXACT_CACHE_MISS')
          PY

      - name: Install prefetch dependencies
        if: steps.exact.outputs.ready != 'true'
        run: npm ci

      - name: Prefetch genuine Binance USD-M history
        if: steps.exact.outputs.ready != 'true'
        env:
          PERP_RESEARCH_START_DATE: 2022-09-01T00:00:00Z
          PERP_RESEARCH_END_DATE: 2026-07-01T00:00:00Z
        run: npx tsx scripts/research-lab-prefetch-event-regime-v53.ts

      - name: Compile corroboration harness
        run: python -m py_compile scripts/research_causal_handoff_clean_sheet_v4.py scripts/research_avax_v4_prehistory_corroboration.py

      - name: Run frozen AVAX V4 prehistory
        env:
          RESEARCH_STATE_DIR: .research-state
        run: python scripts/research_avax_v4_prehistory_corroboration.py

      - name: Assert diagnostic-only fail-closed
        shell: bash
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          d=json.loads(Path('.research-state/avax-v4-prehistory-corroboration.json').read_text())
          assert d['researchOnly'] is True
          assert d['productionChanged'] is False
          assert d['vpsChanged'] is False
          assert d['liveChanged'] is False
          assert d['realTradingEnabled'] is False
          assert d['v4LogicFrozen'] is True
          assert d['originalV4GateStillBinding'] is True
          assert d['originalV4GateOverride'] is False
          assert d['freshOosRead'] is False
          assert d['post20260701DataUsed'] is False
          print('DIAGNOSTIC_ONLY_FAIL_CLOSED_PASS')
          PY

      - name: Upload corroboration artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: avax-v4-prehistory-corroboration
          path: |
            .research-state/avax-v4-prehistory-corroboration.json
            .research-state/avax-v4-prehistory-corroboration-trades.jsonl
            .research-state/avax-v4-prehistory-corroboration.md
          if-no-files-found: warn
          retention-days: 30

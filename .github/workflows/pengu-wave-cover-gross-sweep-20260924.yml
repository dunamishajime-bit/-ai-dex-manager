name: PENGU Wave Cover Gross Sweep
on:
  push:
    branches:
      - research/pengu-wave-cover-gross-sweep-20260924
    paths:
      - scripts/research_pengu_wave_cover_gross_sweep_20260924.ts
      - .github/workflows/pengu-wave-cover-gross-sweep-20260924.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  backtest:
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
      - run: npm ci
      - name: Run PENGU gross sweep
        env:
          PRODUCTION_SOURCE_SHA: c6add8d39676584ad9db094d1ad04deb4050ed06
        run: npx tsx scripts/research_pengu_wave_cover_gross_sweep_20260924.ts
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: pengu-wave-cover-gross-sweep
          path: .research-state/pengu-wave-cover-gross-sweep
          retention-days: 90

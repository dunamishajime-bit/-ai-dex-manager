"use client";
import { useEffect, useMemo, useState } from "react";
import { FileText, RefreshCw } from "lucide-react";
type Trade = { id?: string; executedAt?: string; openedAt?: string; sourceSymbol?: string; destSymbol?: string; action?: string; realizedPnlUsd?: number; realizedPnlPct?: number; provider?: string; strategy?: string; side?: string; quantity?: number; executionPrice?: number; commissionUsd?: number; fundingUsd?: number };
export default function HistoryPage() { const [entries, setEntries] = useState<Trade[]>([]); const [loading, setLoading] = useState(true); const [error, setError] = useState<string | null>(null); const load = async () => { setLoading(true); setError(null); try { const response = await fetch("/api/system/trade-history", { cache: "no-store" }); const data = await response.json(); if (!response.ok || !data?.ok) throw new Error("取引履歴を取得できません。"); setEntries(Array.isArray(data.entries) ? data.entries : []); } catch (reason) { setError(reason instanceof Error ? reason.message : "取引履歴を取得できません。"); } finally { setLoading(false); } }; useEffect(() => { void load(); }, []); const closed = useMemo(() => entries.filter((entry) => typeof entry.realizedPnlUsd === "number"), [entries]); return <main className="space-y-4"><header className="panel-gold rounded-[30px] p-5 md:p-7"><div className="flex items-center gap-2 text-[10px] font-semibold uppercase tracking-[0.28em] text-gold-100/75"><FileText className="h-4 w-4" />トレード履歴</div><h1 className="gold-heading mt-3 text-3xl font-black md:text-5xl">実約定・決済履歴</h1><p className="mt-3 text-sm leading-7 text-white/75">Crypto V96とStock V52を区別して表示します。Entry単体は決済済み実績へ含めません。</p><button type="button" onClick={() => void load()} className="mt-4 inline-flex items-center gap-2 rounded-xl border border-white/10 px-3 py-2 text-xs"><RefreshCw className="h-4 w-4" />更新</button></header>{error ? <Notice text={error} /> : null}<section className="panel-gold rounded-[30px] p-5"><div className="mb-3 flex items-center justify-between"><h2 className="font-bold">決済済み {loading ? "読込中" : `${closed.length}件`}</h2><span className="text-xs text-white/50">実データのみ</span></div><div className="space-y-2">{!loading && !closed.length ? <Notice text="決済済みの実約定履歴はまだありません。未決済Entryは損益レビューに含めません。" /> : closed.map((entry, index) => <div key={entry.id || `${entry.executedAt}-${index}`} className="grid gap-2 rounded-2xl border border-white/10 bg-white/[0.03] p-4 md:grid-cols-[1fr_auto_auto] md:items-center"><div><div className="font-semibold">{entry.sourceSymbol || "-"} {entry.side ? `／${entry.side}` : ""}</div><div className="text-xs text-white/50">{entry.strategy || "Strategy未分類"} ・ {entry.provider || "source未分類"}</div><div className="text-xs text-white/50">{formatDate(entry.openedAt)} → {formatDate(entry.executedAt)}</div></div><div className="text-sm text-white/70">数量 {entry.quantity ?? "-"}<br />価格 {entry.executionPrice ?? "-"}</div><div className={Number(entry.realizedPnlUsd) >= 0 ? "text-emerald-300" : "text-rose-300"}>{money(entry.realizedPnlUsd)}<div className="text-xs">{pct(entry.realizedPnlPct)}</div></div></div>)}</div></section></main>; }
function formatDate(value?: string) { return value ? new Date(value).toLocaleString("ja-JP") : "-"; }
function money(value?: number) { return typeof value === "number" ? `$${value.toFixed(4)}` : "-"; }
function pct(value?: number) { return typeof value === "number" ? `${value >= 0 ? "+" : ""}${value.toFixed(2)}%` : "-"; }
function Notice({ text }: { text: string }) { return <div className="rounded-2xl border border-dashed border-white/10 px-4 py-6 text-center text-sm text-white/60">{text}</div>; }

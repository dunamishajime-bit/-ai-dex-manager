"use client";
import { Activity, ShieldCheck } from "lucide-react";
export function LiveDecisionPanel({ compact = false }: { compact?: boolean }) { return <section className={`panel-gold rounded-[28px] p-4 ${compact ? "" : "md:p-5"}`}><div className="flex items-center gap-2 font-bold"><Activity className="h-4 w-4 text-gold-100" />LIVE判定</div><div className="mt-4 grid gap-3 md:grid-cols-3"><div><div className="text-xs text-white/50">Runner</div><div className="mt-1 font-semibold">V96 Crypto + V52 Stock</div></div><div><div className="text-xs text-white/50">状態</div><div className="mt-1 font-semibold text-amber-200">最終確認待ち</div></div><div><div className="text-xs text-white/50">Executor</div><div className="mt-1 font-semibold">AsterDirectTradeExecutor</div></div></div><div className="mt-4 flex items-center gap-2 rounded-2xl border border-white/10 bg-white/[0.03] p-3 text-xs text-white/65"><ShieldCheck className="h-4 w-4 text-gold-100" />VPS状態を取得できない場合はUNKNOWNとして扱い、稼働中と断定しません。</div></section>; }

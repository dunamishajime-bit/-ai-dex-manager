# XServer VPS Note

The dashboard endpoint is a standard Next.js Route Handler. It is compatible with the existing XServer VPS deployment as long as DisdexManager is run with a Node server (`next start`) rather than static export only.

The feature does not depend on Vercel-specific environment variables or services.

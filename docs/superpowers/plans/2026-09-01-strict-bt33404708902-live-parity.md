# Strict BT #33404708902 Live Parity Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement the verified Source Run #33404708902 gross/MTM execution-plan contracts in production-compatible shared code while keeping Quality102 fail-closed because its live selector is not proven reproducible.

**Architecture:** Add a side-effect-free strict portfolio planner shared by strategy callers. It validates complete inputs, preserves base-strategy priority, counts Quality102 inside Crypto Gross, and emits a mark-to-market reduction plan for lower-priority Quality102 positions rather than zero-PnL trims. Add an explicit Quality102 live-selector gate that rejects frozen historical signals and reports `QUALITY102_LIVE_SELECTOR_PARITY=FALSE`; existing runners remain non-activated and their live gates stay intact.

**Tech Stack:** TypeScript, Node `assert/strict`, existing `tsx` self-test scripts, existing Aster portfolio types and runtime configuration.

**Spec:** User-provided “DisDex 最新厳密BTロジック 実装依頼” in this task; Source Run #33404708902 and SHA `aec066fefd761b12f07e6927b5f2a524f88ca08b`.

## Global Constraints

- Source of Truth is immutable: `SOURCE_RUN=33404708902`, `SOURCE_SHA=aec066fefd761b12f07e6927b5f2a524f88ca08b`.
- `QUALITY102_POSITION_CAP=0.50`, `CRYPTO_GROSS_CAP=2.00`, `TOTAL_GROSS_CAP=2.50`, and stock Gross cap is `1.50`.
- Quality102 is included in Crypto Gross and is lowest priority; V12/PENGU/V52 are not blocked by it.
- Gross reductions use timestamp mark-to-market realized PnL; zero-PnL reduction is forbidden.
- Missing/stale/invalid market, position, order, ownership, or timestamp data fails closed.
- No fixed historical Quality102 CSV or fixed timestamp may be used as a LIVE selector.
- No LIVE activation, VPS deployment, synthetic order, test order, or forced position close is allowed in this branch.

---

### Task 1: Establish failing strict-contract tests

**Files:**
- Create: `scripts/disdex-strict-bt-gross-selftest.ts`
- Create: `scripts/disdex-quality102-live-selector-selftest.ts`

**Interfaces:**
- Tests will consume `STRICT_BT33404708902`, `planStrictPortfolio`, `markToMarketReducePosition`, and `evaluateQuality102LiveSelector`.

- [x] **Step 1: Write the failing tests**

  Cover literal boundary fixtures for Crypto `1.99/2.00/2.01`, Total `2.49/2.50/2.51`, simultaneous base intents, Quality102 residual admission, long/short profitable and losing MTM reduction, remaining quantity/cost basis, duplicate ownership, stale prices, and the Quality102 fail-closed result.

- [x] **Step 2: Run the tests to verify RED**

  Run: `npx tsx scripts/disdex-strict-bt-gross-selftest.ts` and `npx tsx scripts/disdex-quality102-live-selector-selftest.ts`.

  Expected: module/function import failures because the strict production interfaces do not yet exist.

### Task 2: Implement the shared strict gross and MTM planner

**Files:**
- Create: `config/disdexStrictBt33404708902Runtime.ts`
- Create: `lib/disdex-strict-portfolio-planner.ts`
- Modify: `lib/disdex-unified-portfolio-routing.ts`

**Interfaces:**
- `STRICT_BT33404708902` exposes source identity and immutable caps.
- `markToMarketReducePosition(input): MarkToMarketReduction` returns realized PnL, fee/funding cost, remaining quantity, unchanged average entry price, and an audit record.
- `planStrictPortfolio(input): StrictPortfolioPlan` validates snapshots, orders base strategies before Quality102, admits only residual Quality102, and returns accepted/rejected intents, MTM reductions, and gross totals.

- [x] **Step 1: Implement the minimum pure MTM function**

  Use side-adjusted return, entry/mark notional, explicit fee/funding inputs, and reject non-positive prices, invalid sides, reductions larger than the position, and non-finite timestamps.

- [x] **Step 2: Implement strict input validation and gross accounting**

  Require positive equity, unique managed ownership, fresh positive mark prices, valid strategy/symbol mapping, and compute `Crypto Gross = V12 + PENGU + Quality102` and `Total Gross = Crypto + Stock` from mark notional.

- [x] **Step 3: Implement base-priority residual Quality102 handling**

  Sort existing base sleeves before Quality102, preserve admitted base intent, shrink only Quality102 to the minimum of residual Crypto and Total capacity, and emit MTM reduction records at the decision timestamp.

- [x] **Step 4: Update the existing unified policy adapter**

  Keep the existing public `planUnifiedPortfolio` API compatible while using the strict caps (`V12 total 1.50`, PENGU `0.75`, stock `1.50`, Crypto `2.00`, Total `2.50`) and treating Quality102 as a recognized but live-blocked supplemental sleeve.

- [x] **Step 5: Run the tests to verify GREEN**

  Run both self-tests and confirm all boundary, MTM, duplicate, and fail-closed assertions pass.

### Task 3: Add the Quality102 LIVE selector gate and strategy integration hooks

**Files:**
- Create: `lib/disdex-quality102-live-selector.ts`
- Modify: `scripts/disdex-v12-x1-all-live-runner.ts`
- Modify: `scripts/disdex-pengu-dual-ls-v2-live-runner.ts`
- Modify: `scripts/disdex-v13d-v11eq-v96-live-runner.ts`
- Modify: `config/v12X1AllRuntime.ts`
- Modify: `config/penguDualLsV2Runtime.ts`

**Interfaces:**
- `evaluateQuality102LiveSelector(input)` returns `LIVE_BLOCKED_FAIL_CLOSED` unless a production selector manifest explicitly proves no-lookahead, live-available inputs, and Source Run lineage; it never reads a frozen historical signal file.
- Runner integrations may log the Quality102 block and continue V12/PENGU/V52 processing; they must not enable orders or alter existing base signals.

- [x] **Step 1: Add the fail-closed selector implementation**

  Reject absent, stale, fixed-timestamp, fixed-CSV, or incomplete selector manifests with reason codes and expose `QUALITY102_LIVE_SELECTOR_PARITY=false` and `QUALITY102_LIVE_BLOCKED_FAIL_CLOSED=true`.

- [x] **Step 2: Add non-invasive runner hooks**

  Surface the selector state in plan/start telemetry without changing existing live activation gates, order executors, or strategy signals.

- [x] **Step 3: Run selector and runner self-tests**

  Run the new selector self-test plus the existing V12, PENGU V2, V52/V96 contract self-tests in non-LIVE mode.

### Task 4: Regression and parity validation

**Files:**
- Create: `tests/strict_bt33404708902_contract.test.ts`
- Create: `docs/research/strict-bt33404708902-live-parity.md`
- Modify: `package.json`
- Modify: `.github/workflows/strict-bt33404708902-live-parity.yml`

**Interfaces:**
- The contract test runs the real planner and MTM functions with deterministic fixtures; it does not mock the planner.
- The workflow runs typecheck, self-tests, regression tests, and a no-order safety assertion. It does not run a LIVE runner or access VPS credentials.

- [x] **Step 1: Add regression fixtures**

  Verify V12/PENGU/V52 base acceptance parity, simultaneous entry ordering, restart/reconciliation invariants, duplicate rejection, and look-ahead manifest rejection.

- [x] **Step 2: Add the research-only audit document**

  Record the Source Run details, exact MTM accounting, caps, unchanged live gates, and the reason Quality102 is fail-closed.

- [x] **Step 3: Add CI workflow**

  Run Python research-contract tests, TypeScript typechecks/self-tests, and assert `ordersSent=0`, `VPS_PRODUCTION_CHANGED=false`, and `LIVE_ACTIVATED=false`.

- [x] **Step 4: Run the complete local verification suite**

  Run `npm ci`, `npm run typecheck`, all new self-tests, existing strategy self-tests, and the Python Quality102 tests. Record exit codes and failure counts.

### Task 5: Review, commit, push, and CI verification

**Files:**
- Modify: implementation and test files from Tasks 1–4 only.

- [x] **Step 1: Review the diff and source identity**

  Confirm the branch is based on Source SHA, no private key/secret is present, no VPS/LIVE file is changed, and no fixed Quality102 signal is consumed by production code.

- [x] **Step 2: Commit by meaning**

  Create separate commits for tests, core gross/MTM, selector integration, and regression/CI documentation.

- [x] **Step 3: Push the new branch**

  Push `codex/strict-bt33404708902-live-parity` to GitHub without modifying Source SHA or any production branch.

- [x] **Step 4: Wait for CI and inspect the result**

  Record the GitHub Actions Run ID, status, test output, and final status. If CI fails, fix the cause and rerun; do not relax safety gates.

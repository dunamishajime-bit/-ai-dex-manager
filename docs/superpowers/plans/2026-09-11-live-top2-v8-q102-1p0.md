# Live Top2 V8 Q102 1.0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task by task.

**Goal:** Port the verified `V12_TOP2_1.5 + PENGU_V20_V8 + V52_TOP2 + Q102_1.0_1SLOT` contract onto the current production lineage, prove the complete sizing/ownership path, push it, and deploy it only after read-only production gates pass.

**Architecture:** Keep the current production safety and Aster recovery lineage. Add V12 multi-position support through ranked signals, residual sizing, durable multi-position state, and per-position reconciliation/protection. Keep PENGU and V52 signal behavior unchanged. Separate historical Q102 0.5x evidence from the causal LIVE Q102 1.0x/one-slot contract and verify the latter through planner, executor, state, restart, and observability paths.

**Tech Stack:** TypeScript, Node.js/tsx, Python contract tests, systemd immutable releases, PowerShell/OpenSSH.

**Spec:** `docs/superpowers/specs/2026-09-11-live-top2-v8-q102-1p0-design.md`

## Global Constraints

- Never merge the divergent research branch wholesale.
- Preserve Kill Switch, Fail Closed, account lock, reconciliation, REQUEST_WEIGHT budget, and existing execution adapters.
- Do not change V12/PENGU/Q102/V52 signals except the requested V12 Top2 position semantics.
- No synthetic, dummy, forced, or migration test orders; no cancel/flatten for validation.
- Do not deploy while state, ownership, open-order, lineage, or safety reconciliation is unresolved.

---

### Task 1: Pin research evidence and executable contracts

**Files:** research handoff/JSON evidence and Python contract tests from `acf12079509b806809407b454c5c1a115b859907`.

1. Inspect the exact commit file list and import only the specified research evidence and contract tests.
2. Run the imported contracts and record the baseline.
3. Commit the evidence independently.

### Task 2: Implement V12 Top2 configuration and residual planner with TDD

**Files:** `config/v12X1AllRuntime.ts`, `lib/v12-top2-residual.ts`, `lib/v12-x1-all.ts`, associated tests.

1. Add failing tests for max two positions, per-position 1.0x, aggregate 1.5x, distinct ranked symbols, and residual sizing.
2. Implement the minimum ranking/residual logic while preserving existing signal predicates.
3. Run focused tests and commit.

### Task 3: Implement durable V12 multi-position state and restart reconciliation with TDD

**Files:** `lib/v12-x1-all-runner-state.ts`, state/restart tests.

1. Add failing tests for legacy `active` migration, `activePositions`, duplicate symbol rejection, malformed-state Fail Closed, and restart identity.
2. Implement compatible loading, validation, and persistence.
3. Run focused tests and commit.

### Task 4: Integrate V12 Top2 into the LIVE execution engine with TDD

**Files:** `lib/v12-live-execution-engine.ts`, runner wiring, execution/reconciliation/protection tests.

1. Add failing tests for two-position admission, aggregate cap, per-position protection, Q102 priority reduction, and no duplicate ownership.
2. Adapt current production execution code without removing rate-budget, account-lock, reconciliation, or notification behavior.
3. Confirm no order mutations occur in self-tests and commit.

### Task 5: Complete Q102 causal LIVE 1.0x/one-slot contract

**Files:** runtime env example, strict production planner/state validation, observability tests.

1. Add failing tests showing causal LIVE Q102 accepts up to 1.0x, rejects above 1.0x and a second slot, while historical frozen Q102 remains 0.5x.
2. Remove stale production-only 0.5x assumptions and keep Shared Crypto 2.0x / Total 2.5x.
3. Verify MTM resize and restart ownership and commit.

### Task 6: Full regression and release verification

1. Run V12 Top2, PENGU V20/V8, V52 Top2, Q102, shared-risk/reconciliation, watchdog, and stale-state tests.
2. Run Python contracts, TypeScript typecheck, production build, `git diff --check`, and verify real/test/synthetic order counters remain zero.
3. Inspect the final diff against `922668f8...`, commit remaining validation changes, and push the branch.

### Task 7: Read-only VPS preflight, repair, and immutable deployment

1. Record current symlink SHA, units, PID/restarts, state hashes, Kill Switch, account lock, positions, orders, pending state, Gross settings, disk, and lineage.
2. Diagnose the existing failed V12/watchdog without bypassing safety gates; apply only tracked, tested fixes and push if code changes are required.
3. Build an immutable release from the final pushed SHA and validate dependencies/tests on VPS.
4. Reconcile read-only state. Deploy only if all gates pass; restart required units sequentially and never run old/new family instances together.
5. Verify active SHA, ownership, restart stability, natural runner processing, UI observability, and zero migration/test mutations. Roll back or remain Fail Closed if any required gate fails.

